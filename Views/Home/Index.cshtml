@{
    ViewBag.Title = "Inicio";
}

<style>
    .hero-banner {
        background: url('/Images/banner-comida.jpg') center center no-repeat;
        color: white;
        padding: 100px 30px;
        border-radius: 12px;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
    }

    .section-title {
        font-family: 'Georgia', serif;
        color: #5c3d21;
        margin-bottom: 1rem;
    }

    .feature-card {
        border: none;
        background-color: #fff8f0;
    }

    .btn-food {
        background-color: #8B4513;
        color: white;
        border: none;
    }

        .btn-food:hover {
            background-color: #a0522d;
        }
</style>

<main>
    <!-- Banner principal -->
    <section class="hero-banner text-center mb-5">
        <h1 class="display-4 fw-bold">¡Tu comida favorita está a solo un clic!</h1>
        <p class="lead mt-3">Explora una gran variedad de platillos, ordénalos en línea y recíbelos donde estés.</p>
        <a href="@Url.Action("Index", "tblProductoes")" class="btn btn-food btn-lg mt-4">
            Ver Menú <i class="bi bi-arrow-right-circle ms-2"></i>
        </a>
    </section>

    <!-- Sección de ventajas -->
    <div class="container mb-5">
        <div class="row text-center">
            <div class="col-md-4 mb-4">
                <div class="card feature-card h-100">
                    <div class="card-body">
                        <i class="bi bi-bag-check-fill fs-1 text-brown"></i>
                        <h5 class="section-title mt-3">Pedidos Rápidos</h5>
                        <p>Ordena en segundos y recibe tu comida caliente y lista para disfrutar.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card feature-card h-100">
                    <div class="card-body">
                        <i class="bi bi-star-fill fs-1 text-brown"></i>
                        <h5 class="section-title mt-3">Comida de Calidad</h5>
                        <p>Platillos preparados con ingredientes frescos y recetas deliciosas.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card feature-card h-100">
                    <div class="card-body">
                        <i class="bi bi-geo-alt-fill fs-1 text-brown"></i>
                        <h5 class="section-title mt-3">Entrega a Domicilio</h5>
                        <p>Llevamos tu comida favorita hasta la puerta de tu casa u oficina.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Llamado final -->
    <div class="text-center mb-5">
        <h2 class="section-title">¿Aún no tienes cuenta?</h2>
        <p>Regístrate ahora y comienza a disfrutar de la mejor experiencia de pedidos de comida en línea.</p>
        <a href="@Url.Action("Register", "Account")" class="btn btn-outline-dark">
            <i class="bi bi-person-plus-fill me-1"></i> Crear Cuenta
        </a>
    </div>
</main>
